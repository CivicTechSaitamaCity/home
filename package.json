{
  "private": true,
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "build": "npm run build --prefix astro",
    "check": "npm run check --prefix astro",
    "dev": "npm run dev --prefix astro",
    "generate": "npm run build",
    "preview": "npm run preview --prefix astro",
    "postinstall": "npm ci --prefix astro",
    "typecheck": "npm run check --prefix astro"
  }
}
